package dbaccess;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.jdbc.core.RowMapper;import org.springframework.jdbc.core.simple.SimpleJdbcTemplate;import java.sql.ResultSet;import java.sql.SQLException;/** * Created with IntelliJ IDEA. * User: jicui * Date: 1/3/14 * Time: 10:08 PM * To change this template use File | Settings | File Templates. */public class SpitterDAOImpl implements SpitterDAO{    private SimpleJdbcTemplate jdbcTemplate;    @Autowired    public void setJdbcTemplate(SimpleJdbcTemplate jdbcTemplate) {        this.jdbcTemplate = jdbcTemplate;    }    @Override    public void add(Spitter spitter) {        String insert="insert into SPITTER(USER_NAME,PASSWORD,FULL_NAME,EMAIL) values (?,?,?,?)" ;        jdbcTemplate.update(insert, spitter.getUserName(), spitter.getPassword(), spitter.getFullName(),spitter.getEmail());      // spitter.setId(queryForIdentity());    }    @Override    public void remove(Spitter spitter) {        //jdbcTemplate.u    }    @Override    public Spitter getSpitterById(Long id) {        String select="SELECT ID,USER_NAME,FULL_NAME,EMAIL,PASSWORD FROM SPITTER where id=?";      return jdbcTemplate.queryForObject(select, new RowMapper<Spitter>() {          @Override          public Spitter mapRow(ResultSet rs, int rowNum) throws SQLException {              Spitter spitter= new Spitter();              spitter.setId(rs.getLong(1));              spitter.setUserName(rs.getString(2));              spitter.setFullName(rs.getString(3));              spitter.setEmail(rs.getString(4));              spitter.setPassword(rs.getString(5));              return spitter;          }      } ,id);    }}